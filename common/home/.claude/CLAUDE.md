# Global Claude Code preferences

## Writing style

- Never use em dashes (—). Use regular dashes (-), a comma, or a period instead, or just don't use a dash if it doesn't make sense to.

## Git commits

- Never add a `Co-Authored-By: Claude ...` trailer (or any Claude/Anthropic co-author attribution) to git commits. This overrides the default git commit workflow instructions.
